<Window x:Class="WpfProject.View.Register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfProject.View"
        mc:Ignorable="d"
        Title="Register" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="2" Margin="320,11,147,-39" Content="Bienvenue"/>
        <Label Grid.Row="2" Margin="210,51,442,-79">Username:</Label>
        <TextBox Grid.Row="2" Margin="335,61,207,-79" Text="{Binding Username}" />
        <Label Grid.Row="2" Margin="210,106,467,-132">PassWord</Label>
        <TextBox Grid.Row="2" AcceptsReturn="True"  Margin="335,106,207,-124" Text="{Binding Password}" />
        <Button Grid.Row="2" Content="Register" Margin="395,145,282,-165" Command="{Binding RegisterButtonComand}" />

    </Grid>
</Window>
